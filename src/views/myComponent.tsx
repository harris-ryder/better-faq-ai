import React from "react";
import { Layout } from "./Layout";

export const MyComponent = () => (
  <Layout>
    <div>
      <h1 className="text-red-500">Hello, !</h1>
      <p>This is a React component rendered as static HTML.</p>
    </div>
  </Layout>
);
